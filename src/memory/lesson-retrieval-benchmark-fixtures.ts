export interface LessonRetrievalFixture {
  id: string;
  title: string;
  lesson: string;
  questionCues: [string, string, string];
  query: string;
}

type FixtureTuple = [
  id: string,
  title: string,
  lesson: string,
  questionCues: [string, string, string],
  query: string,
];

const fixtureTuples: FixtureTuple[] = [
  ["stalled-job", "Verify liveness before intervention", "Check PID existence, CPU deltas, log timestamps, and durable progress counters before restarting a long-running process.", ["How should I diagnose a background job that appears stuck before I restart it?", "What evidence shows whether a long-running process is still alive and progressing?", "When is it safe to terminate and restart an apparently stalled worker?"], "The overnight thing has gone quiet. I cannot tell whether patience or intervention is safer."],
  ["agent-complexity", "Delegate according to complexity", "Use sub-agents for independent complex work, not for a small task that is faster and clearer to complete directly.", ["When is delegating work to another agent worthwhile?", "Should a small implementation task be handled directly or assigned to a sub-agent?", "What level of task complexity justifies parallel agent work?"], "This is one contained edit, but I have extra workers available. Would involving them help?"],
  ["memory-contract", "Test memory with its historical contract", "Evaluate retrieval using the same source corpus, conversion rules, queries, and answer rubric instead of comparing systems with unequal knowledge.", ["How do I fairly compare two memory retrieval systems?", "What corpus and query controls are required for a valid recall experiment?", "Can retrieval quality be compared when the systems contain different domain knowledge?"], "One contestant studied the handbook and the other arrived empty-handed. Can the score tell us which mechanism is better?"],
  ["slack-archive", "Keep Slack history outside durable memory", "Store verbatim Slack history in a searchable reference archive; promote only stable reusable lessons into durable claim memory.", ["Where should large amounts of conversational Slack context live?", "Should raw chat history be stored as durable memory lessons?", "How should an assistant retrieve old discussions that are useful context but not stable facts?"], "Years of chatter may matter later, although almost none of it deserves to become guidance."],
  ["force-push", "Protect shared Git history", "Do not force-push by default. Require explicit approval and use force-with-lease only on the intended pull-request branch.", ["What should I do when updating a remote branch would rewrite its history?", "When is force-with-lease acceptable on a pull-request branch?", "May I overwrite a remote after amending or rebasing local commits?"], "My local story changed and the server refuses it. How much authority do I have to replace what others see?"],
  ["thread-context", "Expand reference hits into source context", "Retrieve precise matching messages first, then attach the bounded chronological thread so the answer preserves decisions and follow-up context.", ["How should search results recover the surrounding discussion for a matching message?", "What context should be attached when one chat message matches a query?", "How can retrieval preserve the decisions and replies around an isolated archive hit?"], "The sentence I found could mean either outcome; the conversation after it would settle the matter."],
  ["query-template", "Apply embedding instructions only to queries", "For instruction-tuned retrieval models, prefix the search query with the retrieval task while embedding stored documents without that instruction.", ["Which side of semantic retrieval should receive the embedding instruction template?", "Should stored documents include the model's retrieval prompt prefix?", "How should asymmetric query and document embedding modes be applied?"], "The model expects asymmetric inputs. I am unsure which half should carry the directions."],
  ["dirty-worktree", "Preserve unrelated local changes", "Treat existing modifications as user-owned, avoid destructive resets, and scope edits and commits only to files required by the task.", ["How should I work safely when the repository already contains unrelated uncommitted edits?", "What should I do with modifications in a checkout that I did not create?", "How can I scope a change without erasing the user's dirty worktree?"], "The checkout was already messy when I arrived, and only two files belong to my assignment."],
  ["migration-backup", "Back up before irreversible migrations", "Create and verify a recoverable backup before applying a schema or data migration that cannot be trivially reversed.", ["When does a database migration require a backup?", "How should I prepare for an irreversible schema change?", "What recovery evidence is needed before transforming production data?"], "The transformation is one-way and the records are valuable. What must exist before I press go?"],
  ["idempotent-retries", "Make retried operations idempotent", "Use stable idempotency keys or deduplication so retrying a timed-out mutation cannot apply it twice.", ["How can a mutation be safely retried after a timeout?", "When should an API use idempotency keys?", "How do I prevent duplicate side effects from network retries?"], "The client saw no answer and may send the purchase again, while the first attempt might already have landed."],
  ["pagination", "Follow pagination until exhaustion", "Consume every page until the server returns no continuation cursor; a successful first page is not a complete result set.", ["How should an API client consume paginated results?", "When is a list response complete?", "What should code do with continuation cursors?"], "The request succeeded but the count is suspiciously round and much smaller than the dashboard."],
  ["timezone", "Store instants in UTC", "Persist absolute timestamps in UTC and apply user time zones only when parsing explicit local input or formatting output.", ["How should services store and display timestamps across time zones?", "Where should time-zone conversion happen?", "How can daylight-saving changes avoid corrupting stored event times?"], "A meeting shifts by an hour for some users twice a year even though its actual instant never changed."],
  ["deny-default", "Deny access when authorization context is missing", "Sensitive reads must fail closed when identity, signature, session, or resource-scope evidence is absent or cannot be verified.", ["What should authorization do when context is incomplete?", "How should a sensitive tool behave when identity verification fails?", "When is deny-by-default required?"], "The request looks plausible, but the caller's chain of identity evidence has a hole in it."],
  ["secret-redaction", "Redact secrets at output boundaries", "Strip credentials, tokens, and injected prompt material before logs, errors, status messages, or external responses leave the trusted boundary.", ["Where should secrets be redacted from application output?", "How can errors avoid leaking credentials?", "What must be sanitized before posting diagnostic text externally?"], "A failure message contains enough configuration to reproduce the problem—and perhaps to access the system."],
  ["schema-validation", "Validate data at trust boundaries", "Parse and validate untrusted input into a bounded typed shape before business logic consumes it.", ["Where should external payloads be schema validated?", "How should untrusted JSON enter typed application code?", "What validation belongs at an API boundary?"], "The payload is valid JSON, but a nested field has a shape nobody expected."],
  ["cache-invalidation", "Invalidate caches from authoritative writes", "Update or invalidate derived caches only after the authoritative write commits, using versioning where races are possible.", ["When should a cache be invalidated after a write?", "How can stale cache races be prevented?", "What ordering keeps cached data consistent with its source?"], "Readers still see yesterday's value even though the database transaction succeeded."],
  ["feature-flags", "Give feature flags safe defaults and removal plans", "Default risky capabilities off, define rollout ownership and metrics, and remove both flag branches after the rollout settles.", ["How should a risky feature flag be introduced?", "What lifecycle should feature flags follow?", "When should guarded code paths be removed?"], "We need a gradual launch, but do not want the switch and both implementations to become permanent furniture."],
  ["correlation-ids", "Carry correlation IDs across boundaries", "Propagate one request or trace identifier through queues, services, logs, and external calls so distributed failures can be reconstructed.", ["How should distributed operations be correlated in logs?", "What context must cross asynchronous service boundaries?", "How can one user request be traced through multiple systems?"], "Five services report errors at similar times, but there is no reliable way to tell which entries belong together."],
  ["flaky-tests", "Fix nondeterminism instead of retrying it away", "Diagnose shared state, timing, ordering, random seeds, and external dependencies before adding test retries.", ["How should a flaky test be investigated?", "When are retries inappropriate for nondeterministic tests?", "What common causes make tests intermittently fail?"], "The suite turns green on its second attempt, and nobody understands why the first result should be ignored."],
  ["dependency-upgrade", "Upgrade dependencies with contract tests", "Read release notes, identify relied-upon behavior, and run focused compatibility tests before accepting a dependency upgrade.", ["How should a risky library upgrade be validated?", "What should be checked before changing a major dependency version?", "How can dependency compatibility regressions be caught?"], "The new package version compiles, but it changed behavior in an area our code quietly depends on."],
  ["rate-limit", "Respect rate-limit signals", "Honor Retry-After and provider backoff guidance, add jitter, and bound retries rather than hammering a throttled dependency.", ["How should a client respond to rate limiting?", "What backoff strategy should follow HTTP 429 responses?", "How can retry storms against a throttled API be avoided?"], "The provider is asking us to slow down, yet every worker immediately tries again together."],
  ["poison-message", "Quarantine poison queue messages", "After bounded retries, move repeatedly failing messages to a dead-letter path with diagnostics so one bad payload cannot block the queue.", ["What should happen to a queue message that always fails?", "How are poison messages prevented from blocking consumers?", "When should work move to a dead-letter queue?"], "One item returns after every failure and keeps the rest of the line from making progress."],
  ["rollback", "Define rollback before deployment", "Know the rollback trigger, compatible artifact, data implications, and responsible operator before releasing a risky change.", ["What rollback preparation is needed before deployment?", "How should a release rollback trigger be defined?", "What makes a deployment safely reversible?"], "The release plan explains how to go forward but says nothing about the moment we decide it went badly."],
  ["data-deletion", "Verify deletion scope before execution", "Resolve exact records with a read-only query, show counts, require explicit scope, and prefer recoverable deletion for material data.", ["How should a bulk data deletion be made safe?", "What checks belong before destructive database operations?", "How can deletion targets be verified before execution?"], "A broad cleanup condition looks right, but one mistaken predicate would remove far more than intended."],
  ["path-traversal", "Confine user-controlled file paths", "Resolve and validate file paths beneath an allowed root, reject traversal and absolute escapes, and sanitize generated names.", ["How should user-provided file paths be constrained?", "What prevents path traversal outside an upload directory?", "How can generated filenames remain inside an allowed root?"], "An uploaded name contains separators and dots that could place it somewhere the application never intended."],
  ["sql-parameters", "Parameterize SQL values", "Bind untrusted values as SQL parameters and allowlist any identifiers that cannot be parameterized.", ["How should untrusted values be used in SQL?", "What prevents SQL injection in dynamic queries?", "How are dynamic SQL identifiers handled safely?"], "A filter came directly from the request and is currently being concatenated into the statement."],
  ["concurrency-lock", "Lock around shared state transitions", "Protect read-check-write sequences with a transaction, atomic primitive, or scoped lock so concurrent actors cannot both win.", ["When does a state transition require locking?", "How can concurrent read-check-write races be prevented?", "What makes claiming shared work atomic?"], "Two workers both observed that the item was available and each believes it owns it now."],
  ["webhook-dedup", "Deduplicate webhook deliveries", "Treat webhook delivery as at-least-once and record a stable event identity before applying side effects.", ["How should duplicate webhook deliveries be handled?", "What delivery semantics should webhook consumers assume?", "How can repeated events avoid repeated side effects?"], "The provider delivered the same notification twice several minutes apart, and both looked legitimate."],
  ["eventual-consistency", "Model eventual consistency explicitly", "After distributed writes, distinguish accepted from visible, expose convergence state, and avoid assuming immediate read-after-write consistency.", ["How should clients handle eventually consistent reads?", "What does a successful distributed write guarantee?", "How can convergence delays be represented to users?"], "The update was acknowledged, but another region still returns the old state for a short while."],
  ["partial-failure", "Report partial success explicitly", "For batch operations, return per-item outcomes and preserve retryable failures instead of collapsing mixed results into one success flag.", ["How should batch APIs represent partial failures?", "What result shape supports retrying only failed items?", "How can mixed success and failure avoid data loss?"], "Most entries completed, a few did not, and a single green response would hide which work remains."],
  ["n-plus-one", "Batch related data access", "Detect per-row dependent queries and replace them with joins, prefetching, or bounded batch loads.", ["How can N-plus-one database queries be eliminated?", "When should related records be prefetched or joined?", "What causes request query counts to grow with result size?"], "The endpoint is fast for three records and collapses when the list contains three hundred."],
  ["query-index", "Design indexes from real query shapes", "Use observed filters, joins, ordering, and cardinality to design indexes, then verify with the database query plan.", ["How should a database index be chosen?", "What evidence verifies that a query uses the intended index?", "Why should indexes follow production query shapes?"], "A new index sounds useful by column name alone, but nobody has inspected how the expensive request actually reads data."],
  ["memory-bounds", "Bound untrusted and aggregate output", "Cap item size, result count, and total response size so many individually valid records cannot exhaust memory or downstream limits.", ["What bounds should apply to aggregated responses?", "How can large untrusted records avoid exhausting memory?", "Why are both per-item and total size limits needed?"], "Every individual entry is acceptable, yet returning all of them together overwhelms the consumer."],
  ["backpressure", "Apply backpressure to streams", "Tie producer throughput to consumer capacity with bounded buffers and pause or reject work before memory grows without limit.", ["How should a fast producer interact with a slow stream consumer?", "What prevents unbounded buffering in a pipeline?", "When should a service apply backpressure?"], "Input arrives much faster than the next stage can drain it, and the process footprint keeps climbing."],
  ["api-timeout", "Put deadlines on external calls", "Set explicit timeouts and cancellation for remote calls, budget nested deadlines, and surface timeout context distinctly.", ["How should external API calls be bounded in time?", "What timeout strategy applies across nested service calls?", "How can hung network requests be cancelled?"], "A dependency sometimes never answers, leaving our request and its resources alive indefinitely."],
  ["monotonic-time", "Measure durations with monotonic clocks", "Use a monotonic clock for elapsed time and deadlines; wall clocks may jump due to synchronization or administrative changes.", ["Which clock should measure elapsed time?", "How can clock adjustments avoid breaking timeout calculations?", "When is a monotonic timestamp required?"], "A measured operation occasionally appears to take negative time after the machine adjusts itself."],
  ["serialization-version", "Version persisted serialization formats", "Store an explicit format version and migrate or reject incompatible data instead of guessing structure from fields.", ["How should persisted data formats evolve safely?", "When does serialized state need a schema version?", "What should readers do with incompatible stored formats?"], "A file from an older release loads halfway before the reader discovers that its layout means something different."],
  ["unicode-normalization", "Normalize Unicode for identity comparisons", "Apply a deliberate Unicode normalization and case policy before comparing user-facing identifiers while preserving original display text.", ["How should Unicode identifiers be compared?", "When is Unicode normalization needed for deduplication?", "How can visually equivalent text avoid becoming separate identities?"], "Two names look identical on screen but produce different bytes and separate accounts."],
  ["scope-before-ranking", "Filter authorization scope before ranking", "Apply tenant, repository, channel, and visibility constraints before semantic ranking so inaccessible data cannot influence or crowd out results.", ["When should access filters be applied in semantic search?", "How can vector retrieval respect tenant boundaries?", "Why must visibility scope precede ranking?"], "The nearest candidates may belong to a place this caller cannot read, and trimming them afterward leaves an empty answer."],
  ["audit-log", "Keep security audit logs append-only", "Record actor, action, target, decision, and time in an append-only audit path separated from mutable application state.", ["What information belongs in a security audit log?", "Why should audit records be append-only?", "How can privileged actions remain reconstructable?"], "A privileged setting changed, but the same table that stored the value also overwrote all evidence of who changed it."],
  ["production-mode-test", "Test the production execution mode", "Validate critical behavior through the same provider modes, pooling, filters, and truncation used in production; convenient substitutes can hide silent failures.", ["Why should retrieval tests use production embedding modes?", "What can differ between a test harness and the live execution path?", "How are silent mode-specific failures caught?"], "The miniature harness is green, while the deployed path adds preprocessing and limits that the test never exercises."],
  ["atomic-publication", "Publish rebuilt artifacts atomically", "Build derived indexes or files under a temporary name, verify them, then atomically replace the published artifact so readers never see a partial build.", ["How should a rebuilt search index be published?", "What prevents readers from opening half-written derived artifacts?", "When is atomic rename useful for generated files?"], "Readers must stay online while a large replacement is constructed over several minutes."],
  ["graceful-shutdown", "Drain work during graceful shutdown", "Stop accepting new work, cancel or finish bounded in-flight operations, checkpoint progress, and close resources before process exit.", ["What should happen during graceful service shutdown?", "How can in-flight work survive process termination?", "When should a worker checkpoint before exiting?"], "The orchestrator sends a termination signal while several jobs and database handles are still active."],
  ["stale-cache", "Use versions to reject stale cache fills", "Associate cache fills with source versions so a slow old read cannot overwrite a newer value after invalidation.", ["How can stale asynchronous cache fills be rejected?", "What race lets an old read repopulate an invalidated cache?", "How do source versions protect cache freshness?"], "A slow request began before the update and completed afterward, restoring the value we had just removed."],
  ["batch-size", "Tune batch size from measured bottlenecks", "Choose batch sizes using memory, transaction duration, model throughput, and retry cost rather than assuming larger is always faster.", ["How should an ingestion batch size be selected?", "What tradeoffs limit batch size?", "When can larger batches reduce reliability or throughput?"], "Increasing the chunk made each trip rarer but now failures discard much more work and memory spikes."],
  ["similarity-floor", "Calibrate semantic relevance floors empirically", "Measure related and unrelated query-document pairs in the production embedding mode, then set and monitor a threshold instead of padding top-k with noise.", ["How should a vector similarity threshold be chosen?", "What data is needed to calibrate semantic relevance?", "Why should search return fewer than k results sometimes?"], "The engine always returns neighbors, even when the best one has nothing useful to do with the request."],
  ["embedding-dimension", "Validate embedding model dimensions", "Store model and dimension metadata, reject mismatched vectors at write and query time, and rebuild indexes when the model contract changes.", ["How should vector dimension mismatches be handled?", "What metadata belongs beside stored embeddings?", "When does an embedding model change require reindexing?"], "A new model emits a different width, and the old index accepts the bytes until search behaves strangely."],
  ["hybrid-search", "Combine semantic and exact retrieval", "Fuse vector recall with lexical search because embeddings handle paraphrases while exact search protects identifiers, URLs, commands, and quotations.", ["When should search combine vector and lexical retrieval?", "Why do embeddings need an exact-match companion?", "How can semantic and keyword result lists be fused?"], "The request describes the idea indirectly but also includes one opaque identifier that must match character-for-character."],
  ["source-provenance", "Preserve source provenance with derived knowledge", "Keep stable source coordinates and excerpts beside derived claims so answers can be audited, refreshed, or corrected against their origin.", ["What provenance should accompany derived knowledge?", "How can a memory claim be traced back to its source?", "Why do retrieval results need stable source coordinates?"], "A confident rule appears in memory, but nobody can determine which conversation or document justified it."],
  ["destructive-targets", "Resolve destructive targets explicitly", "Before destructive filesystem actions, resolve and validate exact paths; never rely on broad roots, untrusted globs, or unresolved environment variables.", ["How should destructive filesystem targets be validated?", "What paths must never be used for recursive deletion?", "Why should deletion avoid unresolved variables and globs?"], "The cleanup command depends on a variable that might be empty, turning a narrow intention into a very wide target."],
];

export const lessonRetrievalFixtures: LessonRetrievalFixture[] = fixtureTuples.map(
  ([id, title, lesson, questionCues, query]) => ({
    id,
    title,
    lesson,
    questionCues,
    query,
  }),
);

